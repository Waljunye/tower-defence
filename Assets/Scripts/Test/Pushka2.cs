using UnityEngine;
namespace Tests
{
    public class Pushka2 : Towers
    {
        // Start is called before the first frame update
        void Start()
        {
           
            Debug.Log(Damage);
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
